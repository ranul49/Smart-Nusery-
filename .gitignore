# dependencies
**/node_modules/

# secrets & local env
**/.env
backend/data/*.json
*.log

# Expo / React Native build artifacts (regenerated by `expo prebuild`)
app/.expo/
app/android/
app/ios/
app/dist/
app/web-build/

# built APKs (large binaries — don't commit)
*.apk

# OS
.DS_Store
Thumbs.db
